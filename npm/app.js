// npm init to create a package.json file
// npm install moment --save to download moment package

// the ^ in front of version means that it's ok for npm to update anything in version 2. but dont update to version 3

var moment = require('moment') // dont need ./ because app knows to look in node modules folder